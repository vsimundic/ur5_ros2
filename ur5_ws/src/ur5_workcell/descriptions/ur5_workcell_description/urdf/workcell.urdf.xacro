<?xml version="1.0"?>
<robot xmlns:xacro="http://www.ros.org/wiki/xacro" name="ur5_workcell">
  <xacro:arg name="name" default="ur5_workcell"/>
  <xacro:arg name="ur_type" default="ur5"/>
  <xacro:arg name="tf_prefix" default=""/>

  <xacro:arg name="joint_limit_params"
    default="$(find ur_description)/config/$(arg ur_type)/joint_limits.yaml"/>
  <xacro:arg name="kinematics_params"
    default="$(find ur5_workcell_description)/config/ur5_calibration.yaml"/>
  <xacro:arg name="physical_params"
    default="$(find ur_description)/config/$(arg ur_type)/physical_parameters.yaml"/>
  <xacro:arg name="visual_params"
    default="$(find ur_description)/config/$(arg ur_type)/visual_parameters.yaml"/>

  <xacro:arg name="safety_limits" default="false"/>
  <xacro:arg name="safety_pos_margin" default="0.15"/>
  <xacro:arg name="safety_k_position" default="20"/>
  <xacro:arg name="force_abs_paths" default="false"/>

  <xacro:arg name="include_camera" default="true"/>
  <xacro:arg name="include_tactile_sensor" default="true"/>
  <xacro:arg name="include_cable_plugs" default="true"/>
  <xacro:arg name="include_table" default="true"/>
  <xacro:arg name="table_width" default="0.9"/>
  <xacro:arg name="table_length" default="1.5"/>
  <xacro:arg name="table_height" default="0.9"/>
  <xacro:arg name="table_robot_edge_offset" default="0.21"/>
  <xacro:arg name="articulated_gripper" default="true"/>
  <xacro:arg name="grasp_tcp_xyz"
    default="-0.05091168824543142 0.05091168824543142 0.275"/>
  <xacro:arg name="grasp_tcp_rpy" default="0 0 -0.7853981633974483"/>

  <xacro:include filename="$(find ur5_workcell_description)/urdf/ur5_workcell_macro.xacro"/>
  <xacro:include filename="$(find ur5_workcell_description)/urdf/workcell_environment.xacro"/>

  <!-- The installed UR macro requires a parent; reusable users can choose another. -->
  <link name="world"/>
  <link name="$(arg tf_prefix)workcell_reference"/>
  <joint name="$(arg tf_prefix)workcell_reference_joint" type="fixed">
    <parent link="world"/>
    <child link="$(arg tf_prefix)workcell_reference"/>
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </joint>
  <xacro:ur5_workcell
    name="$(arg name)"
    parent="$(arg tf_prefix)workcell_reference"
    tf_prefix="$(arg tf_prefix)"
    joint_limits_parameters_file="$(arg joint_limit_params)"
    kinematics_parameters_file="$(arg kinematics_params)"
    physical_parameters_file="$(arg physical_params)"
    visual_parameters_file="$(arg visual_params)"
    safety_limits="$(arg safety_limits)"
    safety_pos_margin="$(arg safety_pos_margin)"
    safety_k_position="$(arg safety_k_position)"
    force_abs_paths="$(arg force_abs_paths)"
    include_camera="$(arg include_camera)"
    include_tactile_sensor="$(arg include_tactile_sensor)"
    include_cable_plugs="$(arg include_cable_plugs)"
    articulated_gripper="$(arg articulated_gripper)"
    grasp_tcp_xyz="$(arg grasp_tcp_xyz)"
    grasp_tcp_rpy="$(arg grasp_tcp_rpy)">
    <origin xyz="0 0 0" rpy="0 0 ${pi}"/>
  </xacro:ur5_workcell>

  <xacro:if value="$(arg include_table)">
    <xacro:workcell_table
      parent="$(arg tf_prefix)workcell_reference"
      width="$(arg table_width)"
      length="$(arg table_length)"
      height="$(arg table_height)"
      robot_edge_offset="$(arg table_robot_edge_offset)"/>
  </xacro:if>
</robot>
